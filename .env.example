# Stack Ai OS — example secrets. Copy to .env (gitignored) and fill in real values.
# .env is NEVER committed.

# Firecrawl (web crawl/search for research integration)
FIRECRAWL_API_KEY=fc-your-key-here
